# LoggerHelper API Template

Generated with `dotnet new loggerhelper-api`.

## Run

```bash
dotnet run
```

Edit `appsettings.LoggerHelper.json` to route logs by level to Console, File, Email, and more.
